import 'package:assignment/presentation/products/getx/product-controller.dart';

import '../../../config/utils/constants/export.dart';
import '../getx/product-controller.dart';

class DetailsProduct extends GetView<ProductController> {
  const DetailsProduct({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
